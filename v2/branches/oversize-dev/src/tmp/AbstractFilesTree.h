#ifndef ABSTRACTFILESTREE_H#define ABSTRACTFILESTREE_H#include <string>#include <vector>/******************************* Abstract Class ****************************AbstractFilesTree does not have any pure virtual methods, but its author  defined it as an abstract class, so you should not use it directly.  Inherit from it instead and create only objects from the derived classes*****************************************************************************/class AbstractFilesTree{public:	// Constructors/Destructors	//  	/**	 * Empty Constructor	 */	AbstractFilesTree ( );	/**	 * Empty Destructor	 */	virtual ~AbstractFilesTree ( );protected:private:};#endif // ABSTRACTFILESTREE_H